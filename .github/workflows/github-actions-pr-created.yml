name: GitHub Actions PR Created
on:
  pull_request:
    types: [opened]

jobs:
  PR-Created:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
